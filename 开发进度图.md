# Agent 设置竞态修复：开发进度图

> 高危实验归档，禁止作为稳定版本使用。本文记录暂停施工时的实际状态，不是安全验收报告。
>
> 归档日期：2026-09-11（北京时间）。
> 归档目标分支：danger试验b。
> 原始实验分支：danger-experimental-do-not-use。
> 用户要求：暂停当前施工，归档代码与本文，推送新分支后切回原始实验分支。暂不继续自动保存策略改造。

## 一、背景与历史定位

此前调查发现 main 曾从 c20c68fb 回退到 4d0d160e，backup-before-rollback 保留回退前历史。
本轮实验基于 PR #200 合并节点 69577e84bbd9d3fb5e28aa0c1468373da73266c6，并应用 fix3。
fix3 是 UI 修复，不应作为数据库问题的原因或修复依据。

调查重点是快速切换 Agent 时，共享设置表单、单例提示词模块、异步加载和多个保存入口之间的竞态。
配置被覆盖、话题索引变化与聊天历史文件删除是不同问题；本轮尚未证明所有疑似数据库丢失现象均已定位或修复。

## 二、进度图

需求与历史定位：已完成
↓
Agent 独立草稿、不可变快照、保存队列：已实现，模块测试通过
↓
后端版本冲突检查、持锁新鲜读取、历史恢复点：已实现，隔离测试通过
↓
提示词与正则保存入口协调：主要代码已实现，组合边界未验收
↓
全局提示词仓库版本化与队列：已实现，独立前后端测试通过
↓
界面归属恢复、预设任务追踪、切换与卸载屏障：施工中，最新修改待回归
↓
手动保存与自动保存的产品语义：暂停，等待架构决策
↓
真实编辑会话组合回归：未完成
↓
隔离 Electron 运行验收：未执行
↓
发布或合并 main：禁止，尚不满足条件

## 三、已经写入代码的内容

| 工作域 | 当前实现 | 主要入口 |
| --- | --- | --- |
| Agent 编辑会话 | 固定 Agent 身份、冻结草稿与提交快照、字段序列、串行保存、失败保留、冲突阻止盲重试 | [编辑会话](modules/settings/agent-edit-session.js) |
| 设置管理 | 输入暂存、会话版本建立、隐藏设置页不随聊天导航回填、切换前保存、手动与自动提交不再重复整表写入 | [设置管理器](modules/settingsManager.js) |
| 保存后界面刷新 | 串行刷新列表与聊天配置，检查会话和版本，不回填编辑表单 | [设置管理器](modules/settingsManager.js)、[列表管理器](modules/itemListManager.js) |
| 提示词上下文 | 子模块上下文隔离、空值保护、迟到回调检查、模式队列、同步快照 | [提示词管理器](Promptmodules/prompt-manager.js)、[文本模块](Promptmodules/original-prompt-module.js)、[模块模式](Promptmodules/modular-prompt-module.js)、[预设模块](Promptmodules/preset-prompt-module.js) |
| 后端配置 | 版本冲突检测、锁内重新读盘、损坏文件拒写、随机临时文件、原文件替换前保留内容寻址恢复点 | [配置持久化](modules/utils/agentConfigManager.js)、[Agent IPC](modules/ipc/agentHandlers.js) |
| 正则规则 | 规则纳入同一配置版本提交，保留旧独立规则文件，导入只解析并交给草稿合并 | [Agent IPC](modules/ipc/agentHandlers.js)、[正则 IPC](modules/ipc/regexHandlers.js) |
| 全局仓库 | 读取不创建空文件、保存强制携带版本、独占锁、队列、旧版本备份、冲突保留草稿、加载失败禁用入口 | [仓库存储](modules/utils/globalPromptWarehouseStore.js)、[提示词 IPC](modules/ipc/promptHandlers.js)、[模块模式](Promptmodules/modular-prompt-module.js) |
| 头像与页面生命周期 | 裁剪回调上下文检查、配置成功后保存头像、失败保留裁剪稿、离开设置页前保存屏障 | [头像选择器](modules/ui-system/avatar-picker.js)、[界面管理器](modules/uiManager.js) |
| 脚本加载 | 设置管理器之前加载 Agent 编辑会话 | [主页面](main.html) |

## 四、暂停前最后修改：尚未统一验证

- 设置显示流程开始向上传递回填失败；仍完整可用的旧编辑器可以恢复界面归属。
- 标题推迟到回填成功后发布；导演词临时输入不再在切换请求刚开始时清空。
- 设置刷新复用正常显示流程，读取后重新检查上下文，失败不清除待刷新标记。
- 全局仓库存在未保存草稿或冲突时，上层提示词上下文提前拒绝切换。
- 非模块模式保存也检查全局仓库草稿。
- 预设内容加载新增任务追踪与等待入口。
- 提示词管理器新增统一等待入口，并接入 Agent 切换和离开设置页。
- 相同内容重复暂存不再重启自动保存计时器。
- 失败切换测试新增真实 surface 归属、旧名称草稿与编辑器可用性断言。

这些修改不能仅凭此前测试通过就标为已验收。

## 五、未完成及已知风险

### 1. 保存按钮与自动保存的架构决策

用户提出：“不是有一个保存按钮吗？为何每次要自动保存呢？”

截至归档时：

- 500ms 输入自动保存仍在代码中，没有移除。
- 切 Agent、离开设置页仍可能触发保存。
- 模块操作与全局仓库仍存在独立提交行为。
- 尚未实现统一的“保存 / 放弃 / 取消切换”交互。
- 助手提出过改为手动保存，但用户随即暂停施工；不得把该提议当成已完成或最终确认的架构。

下一阶段应先明确：普通编辑、模式切换、预设选择、全局仓库、离开设置页和关闭窗口分别何时允许落盘。
不要继续默认“所有切换都必须自动提交”。

### 2. 模式切换事务与统一等待

- 当前会话路径的模式切换仍以暂存成功更新显示模式，没有完整等待持久化确认。
- 普通手动与自动保存尚未全部接入同一异步等待边界。
- 必须避免模式队列等待普通保存，而普通保存反过来等待模式队列的循环等待。
- 统一等待期间新操作、新输入、外部刷新等仍需组合测试。
- 子模块拒绝切换后，设置表单 readiness、标题、surface 归属与提示词父子身份应始终一致；当前仍需全面核对。

### 3. 头像与其他旧回调

- 头像多次并发保存、颜色回调、重置颜色回调和后端文件替换的安全边界仍未全部闭合。
- 预设目录浏览和刷新、模块拖拽、弹窗及同 Agent 模式往返的旧控件归属仍需审查。
- 页面退出不能假定异步保存一定完成。

### 4. 持久化兼容与恢复

- 配置旧保存入口并非全部强制 CAS。
- 正则首次迁移时，旧独立规则文件变化未完整纳入配置版本。
- 文件锁仅保护遵循同一协议的写入方，不保证拦截任意外部编辑器直接写文件。
- 未实现完整掉电一致性保证、恢复点保留策略及显式冲突解决界面。
- 全局仓库与 Agent 私有配置并非跨文件原子事务；例如从私有区移到全局仓库时，一侧成功、另一侧失败的恢复语义仍需设计。
- 全局仓库冲突当前会阻止盲重试，但用户如何保留、导出或合并冲突草稿尚未完成。

## 六、测试证据与限制

以下数字是不同时间、不同测试组合的运行结果，有重复，不能相加：

| 阶段 | 结果 | 限制 |
| --- | --- | --- |
| 早期 Agent 会话、持久化与提示词定向测试 | 24/24 通过 | 不包含后续仓庫和屏障修改 |
| 设置交互与桥接兼容测试 | 68/68 通过 | 部分走旧保存路径，没有加载真实编辑会话 |
| 保存后界面刷新接入后的组合运行 | 92/92 通过，4 个脚本语法检查通过 | 不等于新增刷新路径已被完整覆盖 |
| 新仓库存储接入后的定向运行 | 29/29 通过 | 当时前端夹具尚未覆盖版本队列 |
| 前端仓库队列和冲突测试补齐后 | 21/21 通过，模块语法检查通过 | 独立模块测试，不是完整应用验收 |
| 上层上下文预检查加入后 | 52/52 通过，3 个脚本语法检查通过 | 这是最后一次成功的相关组合运行；之后仍有修改 |

后续两次测试请求因工具标签格式错误在终端解析阶段失败，不能记为测试执行成功，也不能作为代码测试失败证据。
最新屏障、刷新和失败归属测试修改之后，尚未进行完整回归。

主要测试文件：

- [Agent 会话测试](tests/agent-edit-session.test.mjs)
- [Agent 配置持久化测试](tests/agent-config-persistence.test.mjs)
- [全局仓库存储测试](tests/global-prompt-warehouse-store.test.mjs)
- [提示词上下文测试](tests/prompt-context-isolation.test.mjs)
- [设置交互测试](tests/settings-elements-interaction.test.mjs)
- [设置桥接测试](tests/uiux-settings-bridge-modules.test.mjs)

未执行真实 Electron 运行测试，未对日常 Agent 和聊天数据做安全验收。
既有测试输出中的故障注入错误属于预期；JSDOM 未实现功能和夹具缺失 API 的警告不能视作实际应用正常运行的证据。

## 七、下一阶段建议顺序

1. 先讨论保存按钮、自动保存与内存草稿的产品及架构契约。
2. 按确定的契约收敛保存入口，而不是继续叠加隐式提交。
3. 补齐切换、卸载、刷新、失败与冲突状态机。
4. 使用真实设置管理器、编辑会话、三种提示词模块及 CAS 模拟后端建立组合测试。
5. 集中执行语法检查、定向与兼容回归，检查最终差异。
6. 仅在隔离数据目录上进行 Electron 测试；运行前确认所有后端、插件和子进程的数据路径。
7. 验收完成前不合并 main，不将实验分支用于日常数据。

## 八、归档范围

本次归档包含当前已编写的源码、测试和本文；不包含被 Git 忽略的日常运行数据。
推送成功后切回 danger-experimental-do-not-use，原分支不合入本轮工作。
因此切回后根目录本文和新增代码可能不再出现；应从 danger试验b 查看归档。